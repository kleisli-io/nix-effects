# Bridges between freer `Pure` / `Impure` Computations and FreeFx
# description-level values, so the eleven existing effect handlers can be
# reused without forking implementations.
{ fx, api, lib, ... }: {
  scope = {
    compat = { };
  };
}
